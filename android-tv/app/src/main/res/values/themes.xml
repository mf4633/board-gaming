<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme only; no AppCompat dependency. Fullscreen and dark so the
         letterbox around a game matches the site's background instead of flashing white. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>
</resources>
